This repository will be pushed to https://github.com/lakshannaveen/3d-sender

Notes before pushing:
- Do NOT commit local `.env` files. They contain secrets (DB credentials, JWT secret).
- This file (`PUSH_README.md`) documents that push was performed by the local developer tools.

If you want me to push from this machine, confirm and I will set the remote and push.
